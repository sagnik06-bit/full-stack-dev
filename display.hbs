<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Signifya Event Registration</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&display=swap" rel="stylesheet">
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Orbitron', sans-serif;
            color: #fff;
            background: linear-gradient(120deg, #1a2a6c, #b21f1f, #fdbb2d);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            min-height: 100vh;
            overflow-x: hidden;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        header {
            background: rgba(0, 0, 0, 0.7);
            padding: 2rem;
            text-align: center;
            border-bottom: 3px solid #0ff;
            box-shadow: 0 10px 30px rgba(0,255,255,0.2);
        }

        header h1 {
            margin: 0;
            font-size: 2.5rem;
            color: #0ff;
            text-shadow: 0 0 10px #0ff, 0 0 20px #0ff;
        }

        table {
            width: 90%;
            margin: 3rem auto;
            border-collapse: separate;
            border-spacing: 0 20px;
            background-color: transparent;
        }

        th, td {
            padding: 1.2rem;
            text-align: center;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 15px;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.6);
        }

        th {
            background: linear-gradient(45deg, #0ff, #00f);
            color: #000;
            font-weight: 700;
            text-transform: uppercase;
            text-shadow: 0 0 8px #fff;
        }

        input.lg {
            width: 90%;
            padding: 10px;
            border: none;
            border-radius: 8px;
            background-color: #111;
            color: #0ff;
            font-family: 'Orbitron', sans-serif;
            text-align: center;
            box-shadow: inset 0 0 10px rgba(0,255,255,0.4);
        }

        input.lg1, input.lg2 {
            padding: 10px 25px;
            border: none;
            border-radius: 40px;
            font-weight: bold;
            font-size: 0.95rem;
            font-family: 'Orbitron', sans-serif;
            color: #fff;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        input.lg1 {
            background: linear-gradient(135deg, #0f0, #0ff);
            box-shadow: 0 5px 15px rgba(0,255,255,0.3);
        }

        input.lg2 {
            background: linear-gradient(135deg, #ff416c, #ff4b2b);
            box-shadow: 0 5px 15px rgba(255,75,43,0.3);
        }

        input.lg1:hover,
        input.lg2:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 25px rgba(255,255,255,0.4);
        }

        footer {
            text-align: center;
            margin-top: 4rem;
        }

        footer img {
            height: 160px;
            border-radius: 18px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.6);
            transition: transform 0.3s ease;
        }

        footer img:hover {
            transform: scale(1.07);
        }
    </style>
</head>
<body>

{{#if status}}
    <script>
        const status = {{status}};
        if (status === 1) {
            alert("Data Updated Successfully!");
        } else if (status === 2) {
            alert("Data Deleted Successfully!");
        }
    </script>
{{/if}}

<header>
    <h1>🌟 Signifya 3D Registration Portal 🌟</h1>
</header>

<table>
    <tr>
        <th>Name</th>
        <th>Update</th>
        <th>Delete</th>
    </tr>
    {{#each data}}
        <form method="post" action="/update">
            <tr>
                <input class="lg" name="id" hidden type="text" value="{{this._id}}">
                <td>
                    <input class="lg" type="text" name="name" value="{{this.name}}">
                </td>
                <td>
                    <input class="lg1" name="btn" type="submit" value="UPDATE">
                </td>
                <td>
                    <input class="lg2" name="btn" type="submit" value="DELETE">
                </td>
            </tr>
        </form>
    {{/each}}
</table>

<footer>
    <img src="https://via.placeholder.com/350x160/0ff/000?text=Signifya+3D+🔥" alt="Event Banner">
</footer>

</body>
</html>
